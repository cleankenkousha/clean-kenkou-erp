# Clean KENKOU ERP - 作業進捗および次回残課題ログ

**最終更新日時**: 2026-09-24 16:35  
**ステータス**: 営業スケジュール管理対象を「担当者・スタッフ管理」の営業担当（role: sales）のみに限定連動（営業担当の増減に自動追従）、本番ビルド検証完了。

---

## 1. 本日完了した作業・進捗サマリー（2026-09-24）

### ⓪ 【完了✅】 スケジュール管理対象の「営業担当限定＆動的連動」対応
- **背景・課題**:
  - スケジュール画面に全スタッフが表示されるのではなく、設定の「担当者・スタッフ管理」で役割が「営業担当（`role: sales`）」のスタッフのみを対象とし、営業担当が増加・変更された場合にも自動で枠が増減するように対応。
- **実装内容 (`src/pages/Schedule.tsx`)**:
  - `targetStaffList` を `profiles.filter(p => p.role === 'sales')` に連動。
  - フィルター、見積予約モーダル、一般予定モーダル等の選択肢を営業担当のみに限定。
  - 営業担当が未登録の場合の親切なガイダンスと設定画面への誘導リンクを設置。

### ① 【完了✅】 営業スケジュール ＆ 事務員見積訪問枠予約カレンダー機能（/schedule）

- **背景・課題**:
  - お客様からの見積依頼電話に対し、事務員が営業マン（廣田さん、原口さん等）の空き時間を電話で確認する往復のタイムロス（折り返し待ち・顧客保留）を解消。
- **実装内容**:
  1. **新規スケジュール画面 (`src/pages/Schedule.tsx`)**:
     - **営業担当タイムライン（日別ビュー）**:
       - 営業担当者（廣田さん、原口さん、中原さん等）が横並びになり、8:00〜19:00の時間帯の空き枠が一目でわかるUI。
     - **週間カレンダービュー**:
       - 月曜〜日曜の7日間の予定一覧を俯瞰できるカレンダー。
     - **予定区分と視覚的カラーコーディング**:
       - 🟧 **見積訪問アポ（案件連動）**: 事務員または営業が入力した顧客見積。
       - 🟦 **外出・移動**: 役所手続き、下見、移動など。
       - 🟪 **打合せ・会議**: 社内・取引先との協議。
       - 🟩 **現場作業・立会い**: 回収同行や立会い作業。
       - ⬜ **休暇・私用 / その他**
  2. **空き枠クリックからの「即時見積訪問アポ登録」フロー**:
     - タイムライン上の空き枠をクリックすると見積訪問予約モーダルが起動。
     - 担当者・時間が自動セットされ、顧客名・電話番号・住所・品目メモを入力して保存。
     - 保存と同時に **新規案件（`jobs`）の作成（ステータス: 顧客検討・見積対応）＆ スケジュール枠の確保** がワンアクションで完了。
  3. **現場・営業マン向けスマホ・タブレット最適化**:
     - 外出先から予定カードをタップして「Google マップでルート案内を開く」「ワンタップでお客様に電話発信」「連動案件詳細（TaskDetailModal）を開く」ボタンを直結配置。
  4. **データ管理フック (`src/hooks/useStaffSchedules.ts`)**:
     - Supabase `staff_schedules` テーブルとのリアルタイム双方向同期（CRUD）。
     - Supabase テーブル未作成環境でも動作する **localStorage 自動フォールバック機構** を備えた二重安全設計。
  5. **DBマイグレーションスクリプト**: `supabase/create_staff_schedules.sql` を作成。

---

### ② 【完了✅】 担当スタッフアカウント管理 ＆ マイタスク絞り込み

- **実装内容**:
  1. **ダッシュボードの担当者絞り込みフィルター (`src/pages/Dashboard.tsx`)**:
     - カンバンボード上部に「担当者（全スタッフ / 未割当 / 個別スタッフ）」セレクトを新設。
     - 自分の担当案件（マイタスク）のみを1タップで抽出・進捗確認可能に。
  2. **設定画面のアカウント管理拡張 (`src/pages/Settings.tsx`)**:
     - スタッフ追加時に「メールアドレス（任意）」も登録可能に。
     - スタッフ一覧テーブルに「ログインメールアドレス」列を追加。
     - 「メールアドレス変更」および「パスワード設定・再発行リンク送信」ボタンを設置。
  3. **ヘッダーからのパスワード変更モーダル (`src/components/ui/Layout.tsx`)**:
     - 画面右上のユーザーアイコンから、ログイン中のスタッフ自身が新しいパスワード（6文字以上）に変更できるモーダルを実装。

---

### ③ 【完了✅】 ナビゲーション統合 ＆ パフォーマンス最適化（Code Splitting）

- **実装内容**:
  1. **ナビゲーションメニュー更新 (`src/components/ui/Layout.tsx`, `src/App.tsx`)**:
     - PC上部メニューおよびモバイルボトムナビに「スケジュール」タブ（アイコン: `Calendar`）を追加。
  2. **コード分割（`React.lazy` + `Suspense`）の導入 (`src/App.tsx`)**:
     - 全ページコンポーネント（Dashboard, Schedule, Jobs, Customers, Settings, SalesAnalytics）を動的インポート化。
     - 初期バンドルサイズを大幅軽量化し、初回ページロードおよびモバイル通信時の表示速度を向上。
  3. **本番ビルド検証 (`npm run build`)**:
     - TypeScript コンパイルエラー 0件。
     - 各ページが個別チャンクとして分割生成され、ビルド正常完了を確認。

---

## 2. 過去の作業ログ（2026-09-18〜2026-09-21）

- **2026-09-21**: 担当スタッフ別認証およびスケジュール機能の要件定義・設計。
- **2026-09-18**: 「日程確定」工程・ステータスの完全連動 & 保存同期不具合解消、Netlify デプロイ検証。
- **2026-09-15**: 売上一覧・乖離分析・経営戦略ダッシュボード（`/sales`）実装。

---

## 3. 次回以降の残課題・今後の推奨タスク

### 【完了✅】Supabase DB マイグレーションの実行

- **`supabase/create_staff_schedules.sql`**:
  - スケジュール管理用テーブル `staff_schedules` および RLS ポリシーの適用が完了し、Supabase クラウド上での全社スケジュール同期基盤が開通しました。

### 🟡【推奨】アクセス制御・RLS（Row Level Security）ポリシーの強化

- 現在 RLS ポリシーは `USING (true)` の全社共有設定となっているため、スタッフ権限別の表示制御（現場スタッフには担当案件のみ等）が必要に応じて適用可能。


### 🟢【運用】ドキュメント・社内マニュアル更新

- 取扱説明書（`docs/取扱説明書_Clean_KENKOU_ERP.md`）へ「スケジュール・見積枠予約」「マイタスク絞り込み」の利用手順を反映。

---

## 4. 本日変更・新規作成したファイル一覧

| ファイル | 区分 | 変更概要 |
|---|---|---|
| `supabase/create_staff_schedules.sql` | 新規 | スケジュール管理テーブル作成用SQLスクリプト |
| `src/types/index.ts` | 変更 | `StaffSchedule`, `ScheduleType` 型定義の追加 |
| `src/hooks/useStaffSchedules.ts` | 新規 | スケジュールCRUDおよび見積枠予約・案件自動作成フック（フォールバック付） |
| `src/hooks/index.ts` | 変更 | `useStaffSchedules` のエクスポート追加 |
| `src/hooks/useProfiles.ts` | 変更 | `Profile` 型に `email` 追加、`addStaff` 拡張、社内デフォルトメール整備 |
| `src/pages/Schedule.tsx` | 新規 | 営業担当タイムライン（日別）＆週間カレンダー画面（即時見積アポ登録・スマホ直結） |
| `src/pages/Dashboard.tsx` | 変更 | 担当者（スタッフ・マイタスク）絞り込みフィルターの追加 |
| `src/pages/Settings.tsx` | 変更 | スタッフ管理テーブルへのメールアドレス列・パスワード設定機能追加 |
| `src/components/ui/Layout.tsx` | 変更 | スケジュールナビゲーション追加、パスワード変更モーダル実装 |
| `src/App.tsx` | 変更 | `/schedule` ルート追加、`React.lazy` + `Suspense` によるコード分割 |
| `docs/current/task.md` | 更新 | 本ログファイル（進捗ログ・完了記録の更新） |

---

*記録者: Antigravity AI Assistant*
